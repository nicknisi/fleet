{
  "projectName": "Session-Grouped Dashboard & Window-Name Statusline",
  "slug": "session-grouped-dashboard",
  "date": "2026-06-10",
  "status": "Draft",
  "supersedes": null,
  "gates": {
    "passed": 5,
    "total": 5,
    "dimensions": [
      {
        "key": "scope",
        "label": "Scope",
        "status": "ready",
        "evidence": "Explicit in/out lists: grouped dashboard + statusline + preview/kill labels in; collapsible groups, sort toggles, and pane-level rows out."
      },
      {
        "key": "risk",
        "label": "Risk",
        "status": "ready",
        "evidence": "Exploration sweep mapped every render site (dashboard.ts:66-96, app.ts:54-82, status.ts:13-36); main risk — header lines breaking the row-index-to-state mapping — is a named prereq in the phase notes."
      },
      {
        "key": "effort",
        "label": "Effort",
        "status": "ready",
        "evidence": "~7 files, one cohesive TUI change with collocated bun tests; fits a single spec/phase."
      },
      {
        "key": "clarity",
        "label": "Clarity",
        "status": "ready",
        "evidence": "User approved concrete previews: grouped headers with urgency-bubbled groups, inline singleton rows, detail-then-branch shrink order, window-with-session-fallback chips."
      },
      {
        "key": "tests",
        "label": "Tests",
        "status": "ready",
        "evidence": "Pass/fail criteria enumerated in this contract as bun test cases for grouping, intra/inter-group ordering, shrink order, singleton rows, and statusline fallback, plus typecheck/lint."
      }
    ]
  },
  "problem": [
    "The fleet dashboard labels each agent with a `session:window` string truncated at a fixed 15 characters, so with multiple agents per tmux session every row reads like `authkit:🤖 wor…` — the window name, the part that actually distinguishes agents, is the part that gets cut off (screenshot from 2026-06-10, 8 agents).",
    "The tmux second statusline renders the raw session name per chip (src/cli/status.ts:26), never adopting the window-aware labels shipped in v0.7.0 (PR #11), so two ready agents in the same session produce identical chips and the user can't tell which window needs attention."
  ],
  "goals": [
    "Dashboard rows are grouped under session header lines, with window names rendered in full (untruncated) at typical terminal widths.",
    "Groups order by their most urgent member so attention-needing agents still bubble to the top; rows within a group order by urgency then window name.",
    "Statusline chips show the window name (falling back to the session name when the window name is empty or equals the session name).",
    "Preview header and kill confirmation use the same window-first labeling rule."
  ],
  "successCriteria": [
    "bun test passes, including new cases: a session with 2+ agents renders a header line plus indented agent rows; a single-agent session renders one inline `session · window` row with no header.",
    "Group ordering test: a session containing a PERMIT agent sorts above a session whose best status is DONE, regardless of alphabetical order. Within-group test: a PERMIT row renders above a DONE row in the same group; equal-status rows order by window name.",
    "Width test: at 120 and 80 columns, a 20-char window name renders untruncated; when narrowing, the detail column shrinks to its minimum and the branch column drops before the window name loses any character.",
    "Statusline test: formatStatusLine renders the window name per chip and falls back to the session name when the window is empty or equals the session name; click ranges (paneId) are unchanged.",
    "Preview header and kill confirmation tests assert the window-first label.",
    "bun run typecheck and bun run lint pass.",
    "Navigation (j/k) and selection skip header lines; the `/` filter hides groups with no matching rows."
  ],
  "scope": {
    "mvp": [
      {
        "item": "Grouped dashboard rendering: session header lines, 2-space-indented agent rows, inline singleton rows",
        "reason": "The core fix for the unreadable name column"
      },
      {
        "item": "Urgency-bubbled group ordering; urgency-then-window ordering within groups",
        "reason": "Preserves the dashboard's attention-first purpose under grouping"
      },
      {
        "item": "Content-sized window column with shrink order detail → branch → window last; all width math uses the existing visibleLength() helper so emoji-prefixed window names pad correctly",
        "reason": "Implements 'never truncate the window' within finite terminal width without one-cell emoji misalignment"
      },
      {
        "item": "Statusline chips: window name with session fallback",
        "reason": "Matches the user's mental model — chips answer 'which window do I jump to'"
      },
      {
        "item": "Preview header and kill confirmation adopt window-first labels",
        "reason": "One consistent labeling rule app-wide"
      },
      {
        "item": "Navigation/selection skip headers; filter hides empty groups",
        "reason": "Grouping must not break existing keyboard flow"
      }
    ],
    "full": [],
    "stretch": [
      {
        "item": "Header line shows per-session agent count and aggregate status icon"
      }
    ],
    "outOfScope": [
      {
        "item": "Collapsible/expandable session groups",
        "reason": "Interaction-model change beyond the readability fix; revisit if session counts grow"
      },
      {
        "item": "User-configurable sort modes",
        "reason": "Urgency-bubbled grouping was chosen explicitly; no evidence a toggle is needed"
      },
      {
        "item": "Pane-level rows (multiple agents in one window)",
        "reason": "State engine keys agents by pane already; the display unit chosen is the window"
      }
    ],
    "future": [
      "Collapsible session groups with persisted state",
      "Session-scoped actions on header rows (kill all, ack all)"
    ]
  },
  "execution": {
    "strategy": "Sequential",
    "phases": [
      {
        "title": "Grouped dashboard & window-name labels",
        "risk": "medium",
        "blocking": true,
        "specPath": "docs/ideation/session-grouped-dashboard/spec.md",
        "notes": "Single cohesive TUI change. Prereq sub-step: introduce a row model (header vs agent rows) decoupling rendered-row index from visibleStates() index in app.ts before the grouping render lands. Then grouping render + ordering in dashboard.ts/app.ts, statusline label in status.ts, label rule in preview.ts/kill.ts, collocated tests throughout."
      }
    ]
  }
}